// TEST 03: Strings — literals, concatenation, length
greeting = "Hello";
name = "MiniLang";
print greeting + ", " + name + "!";

// String length
msg = "compiler";
print length(msg);

// String concatenation with numbers
age = 21;
print "Age: " + age;

// toString conversion
pi = 3.14;
print "PI is: " + pi;
